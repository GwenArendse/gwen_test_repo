#steps to install r, rstudio and git
# re git, download git
# go to rstudio > tools > git/svn and check that the correct path has been specified
#open terminal in r and 
# git config --global user.name 'Your name'
# git config --global user.email 'Your email linked to github acc'
# next, navigate to file > NEW PROJECT > VERSION CONTROL > GIT
# and clone a repo
# commit and push changes